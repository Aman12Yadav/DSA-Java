class Solution {
    public int findDuplicate(int[] nums) {
        int n = nums.length;
        int sum = 0;
        for(int i:nums)
        sum += i;
        int t = ((n)*(n-1))/2;
        return sum-t;
    }
}
