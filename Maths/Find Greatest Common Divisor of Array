class Solution {
    public int findGCD(int[] nums) {
        Arrays.sort(nums);
        int i=1,n=nums.length;
        int min=nums[0];
        int max=nums[n-1];
        for(i=min;i>0;i--)
        {
            if(min%i==0 && max%i==0)
            break;;
        }
        return i;
    }
}
