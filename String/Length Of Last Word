class Solution {
    public int lengthOfLastWord(String s) {
        int i,c=0;
        s=s.trim();
        int n = s.length();
        for(i=n-1;i>=0;i--)
        {
            if(s.charAt(i)==' ')
            break;
            c++;
        }
        return c;
    }
}
