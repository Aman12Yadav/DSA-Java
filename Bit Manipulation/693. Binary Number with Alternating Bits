class Solution {
    public boolean hasAlternatingBits(int n) {
        int b = n&1;
        n>>=1;
        while(n>0){
            int a = n&1;
            if(a!=b)
            b=a;
            else
            return false;
            n>>=1;
        }
        return true;
    }
}
