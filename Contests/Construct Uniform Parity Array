Question:
You are given an array nums1 of n distinct integers.
You want to construct another array nums2 or all even.
of length
In such that the elements in nums2 are either all odd
For each index i, you must choose exactly one of the following (in any order):
• nums2［1］ = nums1［i］
• nums2[i] = nums1[i] - nums|[j], for an index j != i
Return true if it is possible to construct such an array, otherwise, return false.
Example 1:
Input: nums1 = [2,3]
Output: true
Explanation:
• Choose nums2[0] = nums1[0] - nums1[1] = 2 - 3 = -1.
• Choose
nums2 = [-1, 31), and both elements are odd. Thus, the answer is true.

Solution:
class Solution {
    public boolean uniformArray(int[] nums1) {
        return true;
    }
}
